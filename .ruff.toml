# Root Ruff config: delegate to pyproject so CI and local runs share one rule set
# (including C901). Optional [tool.ruff.lint.per-file-ignores] live only in pyproject.toml.
extend = "pyproject.toml"
