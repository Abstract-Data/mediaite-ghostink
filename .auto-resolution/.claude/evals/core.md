# Eval: core

## Capabilities (new behavior)
- "pipeline all-stage execution returns a complete summary payload" — target: pass@3 > 90%

## Regressions (must not break)
- "CLI preserves scrape/extract/analyze/report/all command parsing" — target: pass@1 = 100%
